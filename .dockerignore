# Reinstalled/rebuilt inside the image
node_modules
.next
out
build

# Secrets — note plain `.env` is NOT matched by `.env.*`, so list it too.
.env
.env.*
!.env.example
!.env.*.example

# Not needed in the build context
.git
.gitignore
.dockerignore
Dockerfile
docker-compose.yml
README.md
.vscode
.idea
coverage
*.log
